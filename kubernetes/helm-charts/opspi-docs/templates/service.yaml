apiVersion: v1
kind: Service
metadata:
  name: {{ include "opspi-docs.fullname" . }}
  labels:
    {{- include "opspi-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opspi-docs.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opspi-docs.fullname" . }}-test-connection"
  labels:
    {{- include "opspi-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opspi-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
